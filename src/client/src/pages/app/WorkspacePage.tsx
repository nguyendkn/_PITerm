import useMetaData from "@/hooks/useMetaData";

const WorkspacePage = () => {
  useMetaData("Workspace");

  return (
    <div>
      <h1>Workspace Page</h1>
      {/* Nội dung trang Workspace */}
    </div>
  );
};

export default WorkspacePage;
